[project]
name = "modulogical-backend"
version = "1.0.0"
description = "Atlas backend running as a Cloudflare Python Worker with D1"
requires-python = ">=3.13"
dependencies = [
  "fastapi",
  "argon2-cffi",
]

[dependency-groups]
dev = [
  "workers-py",
  "workers-runtime-sdk",
]
